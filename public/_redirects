# Single-page app fallback. Real files are served directly by Cloudflare
# and must NOT be listed here: a 200 rewrite disables HTTP byte serving,
# which the map archive depends on.
/*    /index.html   200
